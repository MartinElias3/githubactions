name: npm test
on: 
  workflow_dispatch:
               
jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: npm install
        uses: actions/setup-node@v3
        with:
          node-version: lts/*
      - run: npm install