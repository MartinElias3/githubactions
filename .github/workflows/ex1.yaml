name: npm test
on: 
  workflow_dispatch:
               
jobs:
  runs-on: ubuntu-latest
  node: lts/*
  name: npm install
  steps:
    - uses: actions/checkout@v3
    - name: npm install
      uses: actions/setup-node@v3
      with:
        node-version: lts/*
    - run: npm install